const path = require('path');

module.exports = {
  entry: './site/index.js', 
  output: {
    filename: 'bundle.js', // Имя выходного файла
    path: path.resolve(__dirname, 'dist'), 
  },
  mode: 'development', 
};
